#include <stdio.h>

/**
 * main - print the string in thr put function
 *
 * description: using the main function
 * this program prints an input
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of are,\n");
		return (0);
}
